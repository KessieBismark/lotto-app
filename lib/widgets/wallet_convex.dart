import 'package:flutter/material.dart';

class WalletConvex extends StatelessWidget {
  const WalletConvex({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: Container(
            child: Text('Wallet Page'),
          ),
        ),
      ),
    );
  }
}
